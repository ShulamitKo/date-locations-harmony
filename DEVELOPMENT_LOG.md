# יומן פיתוח - מקומות לדייטים
החל מגירסא 10


## גרסה 10 - 27.01.25
### שיפורי אבטחה, לוגים והתראות

#### הגדרות RLS בסופאבייס
- הפעלת אבטחה על טבלת המקומות
- הגדרת מדיניות גישה לצפייה במקומות
- הגדרת מדיניות לעדכון מקומות
- הגדרת מדיניות להוספת מקומות חדשים
- יצירת טבלת לוגים עם אינדקסים לביצועים

#### מערכת לוגים
הוספת מערכת לוגים מקיפה שמתעדת:
- הוספת מקומות חדשים (`SPOT_ADDED`)
- עדכון מקומות (`SPOT_EDITED`)
- פעילות חשודה (`SUSPICIOUS_ACTIVITY`)
- שגיאות מערכת (`ERROR`)

כל רשומת לוג כוללת:
- סוג הפעולה
- נתוני הפעולה
- חומרת האירוע
- זמן
- כתובת IP
- פרטי הדפדפן

#### מערכת התראות
מימוש מערכת התראות באמצעות FormSubmit:
- שימוש במזהה משותף עם טופס יצירת הקשר
- שליחת התראות במקרה של:
  * אירועים חשודים (`severity: 'warning'`)
  * שגיאות מערכת (`severity: 'error'`)
- עיצוב אחיד עם טופס יצירת הקשר

#### שיפור מערכת הכשרות
עדכון והרחבת מערכת הכשרות:
- הגדרת ערכים אפשריים חדשים:
  * `מהדרין`
  * `רבנות`
  * `?` (לא ידוע)
  * `null` (למקומות שאינם מקומות אוכל)
- הוספת אילוצים חכמים:
  * מקומות אוכל חייבים לקבל ערך כשרות תקין
  * מקומות שאינם מקומות אוכל מקבלים אוטומטית `null`
- עדכון הממשק המשתמש:
  * הצגת אפשרויות רלוונטיות בלבד
  * הסתרת שדה הכשרות למקומות שאינם מקומות אוכל
- טיפול בנתונים קיימים:
  * המרת ערכים ישנים לפורמט החדש
  * עדכון אוטומטי של ערכים לא תקינים

#### שיפורים טכניים
- עדכון הטיפוסים ב-TypeScript
- שיפור הלוגיקה בפונקציות עדכון ויצירה
- הוספת לוגים מפורטים לדיבוג
- שיפור טיפול בשגיאות

#### אבטחת קלט
מימוש מערכת מקיפה לאבטחת קלט:
- **ולידציה** (קובץ `src/lib/validation.ts`):
  * בדיקת שם (מינימום 2 תווים)
  * בדיקת כתובת (חובה)
  * וידוא מיקום בתחומי ישראל
  * בדיקת תקינות קטגוריה
  * בדיקת תקינות טווח מחירים
  * בדיקת תקינות סוג כשרות
  * בדיקת תקינות אזור
- **סניטציה** (קובץ `src/lib/security.ts`):
  * ניקוי קלט מ-XSS
  * טיפול בתווים מיוחדים
  * הגבלת אורך טקסט
  * סניטציה אוטומטית בכל הטפסים:
    - הוספת מקום חדש
    - עריכת מקום קיים
    - טופס יצירת קשר
    - הוספת ביקורת

#### משימות להמשך
1. מימוש מערכת גיבויים אוטומטית
2. הוספת מגבלות על כמות הפעולות (Rate Limiting)
3. פיתוח מערכת דיווחים ומעקב אחר תוכן


## גירסא 11- 31.01.25
### מערכת דיווחים ומעקב אחר תוכן

#### מערכת דיווחים - מבנה בסיסי
- הוספת אפשרות דיווח על מקומות לא פעילים/לא מתאימים
- הדיווח מופיע רק בדף הפרטים של המקום
- סוגי דיווחים: ספאם, לא מתאים, סגור, כפול, אחר

#### מערכת סטטוסים
**דיווחים:**
- `pending` - דיווח חדש
- `in_review` - בבדיקת מנהל (אחרי 3 דיווחים)
- `resolved` - טופל
- `rejected` - נדחה

**מקומות:**
- `active` - פעיל
- `under_review` - בבדיקת מנהל (אחרי 3 דיווחים)
- `blocked` - חסום

#### ממשק ניהול
- דף מיוחד למנהל לצפייה וטיפול בדיווחים
- הגישה מוגבלת לפי כתובת IP מורשית
- אפשרות לטיפול בדיווחים (אישור/דחייה)
- הוספת הערות מנהל (`admin_notes`)
- תצוגה ברורה של סטטוס הדיווחים

#### אוטומציה וטריגרים
- טריגר אוטומטי בסופהבייס:
  * משנה סטטוס מקום ל-`under_review` אחרי 3 דיווחים
  * משנה סטטוס דיווחים ל-`in_review`
  * שולח התראות מייל

#### מערכת התראות במייל
- שימוש ב-FormSubmit.co לשליחת התראות
- התראה על כל דיווח חדש
- התראה כשמקום עובר לסטטוס בדיקה



## גרסה 11.5 - 04.02.2024

### ניקוי וארגון הפרויקט
- הוספת תמיכה ב-Supabase CLI לפרויקט
- מחיקת קובץ טיפוסים ישן (`database.types.ts`)
- מחיקת קבצי מיגרציה ישנים שכבר לא רלוונטיים
- יצירת קובץ טיפוסים חדש ומעודכן (`supabase.ts`) שמשקף את המבנה העדכני של הדאטהבייס

### פקודות חשובות
לעדכון טיפוסי TypeScript אחרי שינויים בדאטהבייס:
```bash
supabase gen types typescript --linked > date-locations-harmony/src/types/supabase.ts
```
**מתי להשתמש**: יש להריץ את הפקודה הזו בכל פעם שמבצעים שינוי במבנה הדאטהבייס (הוספת טבלאות, שינוי עמודות, וכו') כדי לוודא שהקוד TypeScript מסונכרן עם המבנה העדכני של הדאטהבייס. 




## גירסא 12- 05.02.25 - הוספת מערכת מגבלות קצב (Rate Limiting)

### תקציר
הוספנו מערכת מגבלות קצב חדשה כדי להגן על המערכת מפני שימוש לרעה ולהבטיח שימוש הוגן במשאבי המערכת.

### שינויים עיקריים

#### מגבלות חדשות לפי סוג פעולה
- **הוספת מקום חדש**: 3 בשעה, 6 ביום
- **עריכת מקום**: 3 בשעה, 5 ביום
- **דיווח על בעיה**: 2 בשעה, 5 ביום
- **הוספת ביקורת**: 3 בשעה, 6 ביום

#### פרטים טכניים
- זיהוי משתמשים מבוסס על כתובת IP
- שימוש בטבלת `rate_limits` בדאטהבייס לניהול המגבלות
- מפתח ייחודי לכל פעולה מורכב מ:
  - סוג הפעולה
  - כתובת IP
  - סוג המגבלה (שעתי/יומי)
- ניקוי אוטומטי של רשומות שפג תוקפן

#### שיפורי UX
- הוספת הודעות שגיאה ברורות למשתמש
- הנחיות למשתמש כאשר מגיע למגבלה
- טיפול במקרים של IP לא מזוהה

### הסבר טכני
המערכת משתמשת בטבלת `rate_limits` עם המבנה הבא:
- `key`: מפתח ייחודי המורכב מסוג הפעולה וה-IP
- `attempts`: מספר הניסיונות
- `expires_at`: זמן תפוגה של המגבלה

כאשר משתמש מבצע פעולה:
1. המערכת בודקת את מספר הניסיונות הקיימים
2. אם המשתמש הגיע למגבלה, הפעולה נחסמת
3. אם לא, הפעולה מתבצעת ומונה הניסיונות מתעדכן

### יתרונות המערכת
- הגנה מפני ספאם והצפת המערכת
- שימוש הוגן במשאבים
- שקיפות מול המשתמשים
- תחזוקה אוטומטית של הרשומות

### שיפורים עתידיים אפשריים
- הוספת מערכת התראות למנהלים על חריגות
- הגמשת המגבלות למשתמשים מאומתים
- הוספת מערכת whitelist לכתובות IP מהימנות 